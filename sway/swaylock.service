[Unit]
Description=Swaylock
Before=hibernate.target sleep.target

[Service]
Type=forking
User=%u
Environment=WAYLAND_DISPLAY=wayland-1 XDG_RUNTIME_DIR=/run/user/%U
ExecStart=bash -c 'env > /tmp/test'

[Install]
WantedBy=hibernate.target sleep.target
